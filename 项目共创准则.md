# PortPilot 项目共创准则（AGENTS.md）

> 本文件是 **PortPilot 项目所有共创智能体（agent）必须遵守的强制准则**。任何 agent 在本仓库协同工作，无论其会话来源，都必须先通读并遵守本准则。
>
> 本准则由「需求 / 验收 / 研发规范 / 仓库治理 / 沙箱机制 / 开发环境 / 编译约束 / 多 Agent 协同规范」八大部分构成，并持续沉淀。当本准则与其它文档冲突时，**以本准则为准**；如发现冲突，应向项目维护者反馈并更新本准则。

---

## 一、需求规范

1. **需求唯一权威**：当前需求依据为 `需求/product-definition/product-definition.html`（PortPilot v2）。前代 SuperConnect 命名线版本（原 `product-definition-v3/v4/v5`）已归档删除，不再保留。
2. **需求必须完整**：任何功能需求必须具备「功能点 + 优先级 + 技术约束 + 验收标准」四要素，缺一不可。
3. **具体功能点属需求文档**：所有具体功能点、技术约束与验收标准（如串口设备选择方式、自定义设备路径、非标准设备枚举、权限错误处理、Qt/QSerialPort 平台约束等）**统一沉淀在需求文档对应章节**（如 `2.2.1 设备选择方式`、`9.3 验收标准`），本准则不重复罗列，只作原则性约束；需求变更时以需求文档为准。

## 二、验收标准规范

1. **DoD 必填**：每项功能都必须有可量化的验收标准（Definition of Done），格式为「Given…，When…，Then…」。
2. **验收标准沉淀**：所有验收标准统一沉淀在需求文档 `9.3 节`，并同步到 GitHub Issue 的验收字段。
3. **未验收不关闭**：任务 Issue 只有在验收标准全部满足后才能关闭。
4. **可测试性**：验收标准必须可观测、可复现，不得使用模糊描述（如"运行良好""基本可用"）。
5. **验收五要素**：任何需求的验收必须同时满足——
   - **符合产品定义**：功能行为与需求文档（`product-definition.html`）一致，不偏离产品定义；
   - **功能可用、好用**：在真实场景下可正常使用，交互顺畅，符合设计与易用预期；
   - **证据链完整**：必须有完整、可回溯的证据证明达到验收标准（测试报告、日志、截图、复现步骤等）；
   - **标准清晰可测**：验收标准本身清晰、可量化、可被独立复验；
   - **留档可追溯**：任何测试必须留下统一格式的测试报告。
6. **多方位评审（评审 agent/Reviewer）**：代码提交/合入前必须经**独立评审 agent（Reviewer）**审核，覆盖以下维度（全部通过方可合入）：
   1. 实现功能：功能按需求与契约正确实现；
   2. 无明显 Bug 引入：未引入明显缺陷或回归；
   3. 内存管理合理：无泄漏、无悬垂、无非法访问；
   4. 编译通过：完整构建无编译错误；
   5. 无明显性能问题：无重大性能劣化；
   6. 接口符合定义、模块功能完善：接口与契约一致，能支撑下一层次的依赖；
   7. 不影响框架结构：不破坏既有的分层、扩展点与架构约束。
   > 评审由独立的**代码评审 agent（Reviewer，`AGENT_ROLE=reviewer`）**执行，评审者与实现者**必须分离**（Dev 不得自审自写）；Reviewer 签发 PASS/FAIL 结论，FAIL 打回 Dev 修复、PASS 作为「待验收」前置证据。机制细则见《协同/agent-collaboration.md》4.3 评审关卡与第六章。
7. **测试报告统一规范**：测试报告分为两类，统一格式、统一归档、可追溯——
   - **人工验收报告**：验收测试（真实场景、边界、权限等）结论统一沉淀在仓库 `测试/reports/` 目录，按「`<功能>-<yyyy-mm-dd>.md`」命名，含：测试对象、测试环境、用例清单、逐条结果（通过/失败+证据）、风险与结论；
   - **自动化测试报告**：CI 阶段由**代码仓库（qtSerial）** 的 GitHub Actions 运行单元/集成测试，产出 JUnit/覆盖率报告，经 `dorny/test-reporter` 发布到 PR 的 Checks 注解，并 `upload-artifact` 归档，按 workflow run ID 可追溯；本仓库为纯文档仓库，不存放测试 CI workflow；
   - **两者结合**：验收时以仓库 `测试/reports/` 报告为权威证据（正文），在对应任务 Issue 评论中附**结论 + 指向仓库报告的链接 + CI run 链接**，形成完整证据链；
   - **看板关联**：GitHub Projects 看板以「列 = 验收状态」呈现（待研发→研发中→待验收→已验收→已关闭），任务卡片移动至「已验收」前必须已附测试报告链接；验收状态同步记录到 Issue/字段。

## 三、研发规范

1. **流程遵守**：遵循 `架构/engineering-process/engineering-process.html` 定义的生命周期（需求→技术设计→迭代开发→验证测试→发布交付→运营反馈）。
2. **质量门禁**：必须通过质量门禁 G1~G5（需求评审/设计评审/代码评审/测试门禁/发布审批）才能进入下一阶段。
3. **代码分层**：代码结构遵循架构分层（核心层 RingBuffer/AppendBuffer/DoubleBuffer → 领域层 Session/Protocol → 服务层 EventBus/ViewManager → UI 适配层 SerialWorker→Qt 信号）。
4. **契约先行**：模块间接口以 `产品/portpilot-design/contracts/` 下的 schema/API 契约为准，实现不得偏离契约。
5. **编码规范**：遵循主流编码规范，命名准确简洁清晰，结构分层合理，预留扩展点，合理处理异常与边界条件。
6. **推送前置（pull --rebase）**：任何 `git push` 之前，必须先执行 `git pull --rebase` 拉取并变基远端最新代码，**确保所有冲突均已解决、工作区无冲突状态**，严禁在未拉取最新代码的情况下直接推送，避免覆盖他人提交。
7. **编译前置（先编译后提交）**：任何 `git commit` 之前，必须对项目执行完整编译/构建并确保**无编译错误**。提交上去的代码必须保证可编译通过；编译失败必须先修复、重新编译通过后方可提交。
8. **CI 通过门槛（提交后等待 CI）**：提交代码并推送之后，**必须等待 CI 流程（如 GitHub Actions）运行成功**，确认 CI 通过后，方可继续后续任务或结束当前对话。若 CI 失败，必须修复后重新提交，直至 CI 通过，不得带着未通过的 CI 继续或收尾。
9. **PR 必须关联任务 Issue**：开 PR 时标题/描述必须用 `Closes #<issue号>`（合入自动关闭）或 `Related #<issue号>`（仅关联）标注对应任务，保证「任务 Issue ↔ PR ↔ CI run ↔ 测试报告」四者互联、验收证据链可互跳；未关联任务 Issue 的 PR 不得进入评审与合入。

## 四、仓库治理规范

1. **分支管理**：
   - `master/main`：稳定主干，始终可发布，仅通过 squash merge 合并 PR。
   - 功能分支：`feat/<模块>`、`fix/<模块>`、`docs/*`，完成后走 PR → 评审 → squash merge。
   - **防污染红线**：任何自动化/平台产物分支（含 trae 自动创建的 `trae/<session>` 分支）与临时产物提交，**一律不得合并或推送回 master/main**，完成临时用途后应删除。
2. **提交规范**：采用 Conventional Commits 风格（`feat/fix/docs/refactor/test/chore: 描述`）。
3. **先拉后推**：推送前必须 `git pull --rebase` 拉取最新并解决冲突，本地编译验证通过后方可推送，严禁覆盖他人提交；具体命令细节与编译/CI 门槛见「三、研发规范」第 6/7/8 条。
4. **禁止直推主干**：不得直接向 master/main 推送未经 PR 的改动。
5. **最小改动**：只提交任务相关文件，禁止 `git add -A` 误纳入无关/敏感文件（.env、凭证、大型二进制）。
6. **组织仓库**：代码与文档以组织仓库 `OneForAll20240313/portpilot-design-docs` 为准（GitHub Pages 域名 `oneforall20240313.github.io`）。

## 五、沙箱机制规范

1. **安全版本库**：所有落地文件先经 `.session_backup`（隐藏 Git 仓库）`git add` + `git commit` 留档，确认安全后再复制到目标目录，确保任何时刻可回溯。
2. **隔离产物**：临时/备份/沙箱文件（`.session_backup`、`.trae-html-share-packages`、`*.log`、`build/`、`dist/`、`release/`）一律加入 `.gitignore`，**禁止提交进仓库**。
3. **外层隔离**：若外层工作目录是 Git 仓库，必须将 `.session_backup` 加入 `.gitignore` 剔除，确保不上传污染版本历史。
4. **防篡改**：将文件安全保存在版本库中，防止被沙箱机制删除、修改或篡改后无法恢复。

## 六、自测开发环境规范

1. **统一环境**：所有 agent 必须使用统一的开发环境与依赖版本，禁止在各自环境引入不一致的工具链。
2. **环境锁定**：依赖版本、构建工具、编译器版本须在配置中锁定（如 CMake 版本、Qt 版本、编译器），保证可复现。
3. **自测前置**：任何代码交付前必须完成本地自测（单元测试 + 构建通过），不得交付未经验证的代码。
4. **测试框架**：统一使用 GoogleTest + GMock 作为单元测试框架。

## 七、编译约束规范

1. **禁止在源码目录编译**：**编译、构建、生成产物一律在独立构建目录（如 `build/`）进行，严禁在源码目录内编译**，避免污染源码目录与版本历史。
2. **构建目录隔离**：构建目录（`build/`、`dist/`、`release/`）必须被 `.gitignore` 排除，不得提交。
3. **构建验证**：代码合入前必须执行完整构建，确保无编译错误，方可推送/合并。
4. **可复现构建**：构建命令与产物路径须在研发文档中明确，保证任何 agent 可复现。

---

## 八、多 Agent 协同规范

1. **看板即唯一事实源**：所有任务的**存在、认领、状态、归属、验收**以 GitHub Projects 看板（组织 `OneForAll20240313`，项目 6）为准。任何 agent 不得凭记忆或本地推测判断任务状态，一律以看板实际数据为准。
2. **显式角色**：每个 agent 会话必须声明角色——**进度监控 agent（PM）**、**代码编写 agent（Dev）** 与 **代码评审 agent（Reviewer）**。角色由外部启动参数强注入（`AGENT_ROLE=pm|dev|reviewer`）；未注入角色时一律降级为**只读巡检**，不得执行任何看板写操作。
3. **能力边界**：PM 做扫描/调度/校验/报告，不实现功能代码；Dev 做认领/实现/测试/PR/移列，不做任务治理（建任务、拆任务、调里程碑）；**Reviewer 做 PR 独立审核并签发 PASS/FAIL 结论，不实现功能、不合入、不移列**。越界动作应被 PM 或评审 subagent 拦截。
4. **分配即加锁（PM 单点）**：多 Dev 共享 token 时 assignee 无法区分彼此（GitHub 对同一用户去重，已实测），**assignee 不能作并发锁**；任务「待研发→研发中」由 PM 单点分配（设 assignee + 移列 + 评论登记 `@<会话名>`），Dev 不自取、只做前置防御检查，已分配任务对其他 agent 静默锁。
5. **状态机合法流转**：状态列仅允许 待研发→研发中→（待修正⇄研发中）→待验收→已验收→已关闭，禁止跳列；「研发中→待验收」前置条件为 **PR 合并 + CI 通过 + 测试报告已附 + Reviewer（代码评审 agent）评审 PASS**；「研发中→待修正」触发条件为 **Reviewer 评审 FAIL**（详见《协同/agent-collaboration.md》4.4）。**任务被领取（认领）的那一刻必须同步标记「研发中」**，不允许"已领取/已开工但状态仍为待研发"的悬挂状态（领取与移列原子同刻）。
6. **验收归属**：「待验收→已验收」由 PM（或其指定的独立验收者）执行验收五要素校验后移动；Dev 无权自行将本人任务移入「已验收」。
7. **进度监控**：PM 定期扫描看板（脚本见附录 A，《协同/agent-collaboration.md》），统计里程碑完成度、各列卡片数、阻塞清单，产出进度报告落盘 `协同/进度/<date>.md`（遵循第五章沙箱机制留档）。
8. **冲突处理**：代码冲突用 `git pull --rebase` 解决；看板冲突（PM 单点分配下不应发生，若重复分配）以 **PM 分配清单 / 任务评论 `@<会话名>` 认领记录**为唯一事实源，后分配者退出；契约文件（`service-api.md` / schema）由 PM 统一维护，Dev 只读引用，避免并行改写竞态。
9. **里程碑闸门（多 Agent 并发）**：多个 Dev 可复用同一组织 token 并发共创，任务互斥靠 **PM 单点分配**（assignee 在共享 token 下不可作锁）；每个 Dev 以 `AGENT_ROLE=dev:<会话名>` 声明唯一标识，归属以任务评论 `@<会话名>` 记录为准。**所有 Dev 只认领当前里程碑（M1）任务**；M1 全部任务进入「已验收/已关闭」前，任何 agent 不得认领 M2 任务；M1 达成即停下，由项目维护者（用户）验收，验收通过后才获批进入下一里程碑（详见《协同/agent-collaboration.md》第八章）。
10. **PM 启动与自举**：PM 角色由项目维护者（用户）以 `AGENT_ROLE=pm` 启动，且全局唯一；用户未启动 PM 前，所有 Dev 仅只读巡检、不执行任何看板写操作，避免无仲裁下的并发。
11. **一次性一件、逐个验收（强制）**：每个 Dev **同时只认领一个任务**，该任务完成并移入「待验收」后**立即停下**，不得自行认领/领取下一个任务，必须等该任务被 PM 验收通过（状态→已验收）后，才允许认领下一个。**禁止"认领完成即停、唯有完成就继续领取"的连轴转模式**；每个任务都要被独立验收，达到"一个一个验收完再继续"。若 PM 已分配下一个而当前任务未验收，Dev 应停下并提示 PM 先验收（详见《协同/agent-collaboration.md》4.1 与 8.3）。
12. **评审 agent（Reviewer）**：PR 合入前必须经独立**代码评审 agent** 审核，核验 PR 是否符合项目约束、是否完善功能，签发 PASS/FAIL；评审者与实现者分离，FAIL 打回修复、PASS 作为「待验收」前置证据（详见《协同/agent-collaboration.md》4.3 评审关卡与第六章）。
13. **看板三关联（Issue ↔ PR ↔ 归属 Dev）**：看板每个任务项必须是可追溯、可归属的完整闭环——
    1. **Issue ↔ PR**：提交 PR 必须用 `Closes #<issue号>` / `Related #<issue号>` 关联任务 Issue；
    2. **Assignees**：PM 分配时 `--add-assignee` 指向目标 Dev 账号，利用看板 Assignees 字段展示归属（共享 token 下 assignee 仅作展示/筛选，不作并发锁，归属事实源仍为评论 `@<会话名>` 记录）；
    3. **归属 Dev 详细身份**：PM 分配评论登记格式 `@<会话名>（AGENT_ROLE=dev:<会话名>）已认领`，Dev 在交付/移列/汇报时以 `dev:<会话名>` 自报身份；评审 agent 以 `reviewer:<会话名>` 在 PR 署名。三者缺一视为看板项不完整，PM 扫描时纠正（详见《协同/agent-collaboration.md》3.4）。
14. **细则**：完整机制（角色表、身份确认、分配命令、评审关卡、状态机、PM/Reviewer 循环、扫描脚本、里程碑闸门、一次性一件）见《协同/agent-collaboration.md》，本准则只作原则性约束，细则以该文件为准。
15. **Dev 六段式进度打点（强制）**：Dev 研发过程必须按阶段在任务 Issue 评论即时打点留痕，让 PM/定时器/用户能判断任务走到哪步、是否仍在进行——①认领→②源码完成→③本地编译通过→④单测通过→⑤提交 PR→⑥ CI 通过；每个打点刷新任务「最后活跃时间」，配合「残局补领判定」（详见《协同/agent-collaboration.md》4.1 与 4.5）。打点是过程留痕，不改变「一次性一件、逐个验收」。
16. **评审结论结构化标记（强制）**：Reviewer 评审结论必须以统一格式签发，首行 `@评审agent 评审结论：【PASS】/【FAIL】`，下分阻塞项/待修项/建议项并附证据；【FAIL】时任务移「待修正」列，并在 Issue 标题加 `[待修正]` 前缀，使任何 agent 认领时能按评论前缀 + 状态列快速识别"该任务已评审完、需修正后再复审"（详见《协同/agent-collaboration.md》4.3 与 4.4）。

---

## 附则

- **沉淀原则**：任何新讨论出的项目决策、约束、硬性标准，都应沉淀到本文件，形成准则。
- **冲突处理**：本准则与其它文档冲突时，以本准则为准。
- **评审机制**：本准则的每一条均应经 subagent 独立评审，确保可执行、有据可依。
- **适用范围**：本文件适用于 PortPilot 项目内所有人员与所有共创智能体（含 trae agent、subagent、外部 agent）。